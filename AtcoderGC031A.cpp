#include <bits/stdc++.h>
#include <vector>
using namespace std;

int cnt = 0;
int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n;
    cin >> n;
    string input;
    cin.ignore();
    getline(cin, input);

    cout << --cnt;


    return 0;
}