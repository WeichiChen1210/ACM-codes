#include <bits/stdc++.h>
using namespace std;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    string a;
    char b;

    cin >> a;
    a[0] = toupper(a[0]);
    cout << a << endl;

    return 0;
}