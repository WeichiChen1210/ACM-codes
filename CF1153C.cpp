#include <bits/stdc++.h>
using namespace std;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int len;
    cin >> len;
    char str[len];
    for(int i = 0; i < len; i++)
        cin >> str[i];
    
    

    return 0;
}